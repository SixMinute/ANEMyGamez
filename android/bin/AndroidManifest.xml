<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="air.com.sixminute.anemygamez"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- START MySDK required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.SEND_SMS" />
	<uses-permission android:name="android.permission.READ_PHONE_STATE"/>
	<uses-permission android:name="android.permission.READ_CONTACTS" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.BLUETOOTH" />
	<uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
	<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />    
    <!-- END MySDK required permissions -->
    
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <!-- 
    Set application 'android:name' to Application class that 
    has 'System.loadLibrary("megjb");' called in 'onCreate()'. 
    -->
    <application
        android:allowBackup="true"
        android:name="com.mygamez.demo.MyApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        
        <!-- ChannelID -->
		<meta-data
		   android:name="excode"
		   android:value="2527" />
        
        <!-- Billing Provider Opening Activity -->
		<activity
		    android:name="cn.cmgame.billing.ui.GameOpenActivity"
			android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
			android:screenOrientation="portrait">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
			<intent-filter>
				<action android:name="android.intent.action.CHINAMOBILE_OMS_GAME" />
				<category android:name="android.intent.category.CHINAMOBILE_GAMES" />
			</intent-filter>
		</activity>
		
		<!-- MyGamez AutoUpdate Activity -->
		<activity
			android:name="com.mygamez.services.MyGamezCenterActivity"
			android:configChanges="orientation|keyboardHidden"
			android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
			android:screenOrientation="portrait"
			android:exported="false">            
		</activity>
		
		<!-- MyGamez Splash Screen Activity -->
        <activity 
           android:name="com.mygamez.splashscreen.MySplashScreen" 
           android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen" 
           android:screenOrientation="portrait"> 
        </activity>
        
        <!-- Actual Game/Application Activity -->
        <activity
            android:name=".AppEntry"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>
